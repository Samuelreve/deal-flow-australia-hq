// We're not using this file directly as the Notification type is already defined in deal.ts,
// but if we were to fully separate concerns, we would define our notification types here.
// For now we'll keep using the existing Notification type from deal.ts.
export {};
